org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
android.nonTransitiveRClass=true
kotlin.code.style=official

# This property is used by the Mozilla Rust plugin
# It helps locate the NDK in local environments if not in PATH
# rust.ndkVersion=25.2.9519653
